# Curator panel → backend API base URL
VITE_API_URL=http://localhost:8000/api/v1

# Comma-separated list of hostnames this build is allowed to run on.
# Leave empty in dev to disable the guard. In production the GitHub Actions
# workflow injects the right value (curator.notiqlik.uz).
VITE_EXPECTED_HOST=
