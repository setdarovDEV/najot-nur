# ════════════════════════════════════════════════════════════════
#  Catch-all — block any host that isn't in the known list.
#  Prevents random Host headers from reaching internal services.
# ════════════════════════════════════════════════════════════════
server {
    listen 80 default_server;
    server_name _;

    return 444;
}
